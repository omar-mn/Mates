apiVersion: v1
kind: Service
metadata:
  name: front-service
  namespace: mates-app
  labels:
    service: front
spec:
  selector:
    service: front
  ports:
    - port: 80
      targetPort: 80

---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: front-dep
  namespace: mates-app
  labels:
    service: front

spec:
  replicas: 1
  selector:
    matchLabels:
      service: front
  template:
    metadata:
      labels:
        service: front
    spec:
      containers:
      - name: front-pod
        image: 3omar0/mates-front:latest 
        ports:
        - containerPort: 80